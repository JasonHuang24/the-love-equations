# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Logs / temp
*.log

# Python (model training scripts)
__pycache__/
*.py[cod]

# Local tooling
.claude/
.roster-audit/

# Body Calc calibration set (local tooling, not site content)
images/calibration/

# Face Calc calibration set (SCUT-FBP5500 research dataset; local tooling, not site content)
images/calibration-scut/

# LE Lab source corpus (third-party texts; deliberately NOT published)
# Presence and integrity are verifiable via the committed lab-corpus.manifest.json
lab-corpus/

# LE Lab reviewer feedback inbox (flag files carry source excerpts; local triage
# only, like lab-corpus/). Adjudicated cases reach the repo as fixture commits.
lab-feedback/
