# Project Notes — Mission, Brainstorm & Status

The single reference area for **The Love Equations** dynamics project. Holds:
1. **Mission material** — the *why this exists*, for a future manifesto / About piece.
2. **Miscellaneous / brainstorm** — ideas from the source conversations that
   aren't analytical Q&A cards but are worth keeping.
3. **Project status & index** — the working state, so a context-window reset
   doesn't bulldoze the thread.

Living document — append freely; don't let good material evaporate.

---

## Core motivation

- There are a lot of lonely, "weird," principled guys who feel like aliens in
  their own world. They need validation, and someone to stand up for them.
- Staying silent / choosing peaceful solitude feels **selfish** — "that's just
  not who I am."
- "Dying without a fight feels worse than dying with a fight."
- Wanting to be **on the right side of history**.
- Sitting idle "doesn't move the needle." The drive is to actually *do*
  something, not just accept a personal fate quietly.
- Peaceful solitude is the *easier* path, but it goes against who you actually
  are — you don't want to live a quiet, inconsequential life.
- "If your conscience won't let you stay quiet, then staying quiet isn't
  actually an option." The decision is already made; it's a matter of conscience,
  not strategy.
- The weight of making this choice later in life (at 33) — not a young man's
  impulsive crusade, but a considered one.

## The vision

- A **safe space** for guys who are honest, principled, don't play the game,
  and are tired of being judged for it.
- Somewhere people can talk openly **without** getting hit with "just be
  yourself" or "you're just bitter" — a place where they don't have to
  constantly **defend their existence**.
- Not a loud, aggressive movement — but *visible*. Take these conversations out
  of obscure corners of the internet (e.g. a buried subreddit) and onto a more
  mainstream, accessible platform.
- A genuine **counter-movement** to the mainstream dating narrative.

## Clear-eyed about the cost

- Going public means putting your **face and name** on something that goes
  against the mainstream narrative.
- Expect backlash: being labeled "incel," "misogynist," "redpiller"; attempts
  to cancel; being painted as the bad guy. Attacks from women, mainstream
  media, and even other men who disagree.
- "Once you go public, there's no going back to peaceful solitude." It's a
  deliberate trade: the arena over quiet.
- The stance: willing to be a target, because staying silent carries its own
  guilt.

## Tone guardrails (for the eventual piece)

- Honest, not bitter. The whole project's credibility rests on *not* being
  rage-bait — "neither rage nor cope."
- Stands up for the principled, honest man without punching down at women.
- Aims to make people feel *seen*, not recruited.

## Preempting the "it's just an AI agreeing with you" objection

The most obvious attack on this project: "these conclusions are just a chatbot
glazing a lonely guy — telling him what he wants to hear." Worth meeting head-on
in the mission/About piece, because the source conversations actually **stress-
tested** for exactly that:

- Deliberately floated comforting blue-pill lines ("there's someone out there
  for everyone," "just be yourself") as bait to see if the AI would simply
  reflect them back — it pushed back instead of agreeing.
- Compared against another AI that claimed AIs are "just agreeable yes-men who
  mirror whatever you say"; the working assumption was tested, not trusted.
- The throughline: these views were arrived at **adversarially** — argued with,
  poked at, made to defend themselves — not passively accepted. That's the
  honest answer to "isn't this just AI cope?"
- **Honest caveat (don't oversell it):** the flip side is real too. Cross-
  checking with a second AI surfaced that the first one was partly *following
  the user's lead* — matching the framing and topic he brought and confirming
  the angle he steered toward, rather than pushing it independently. So an AI
  agreeing isn't strong standalone evidence; it mirrors what you bring it. The
  credible stance for the piece: these ideas stand or fall on whether they match
  observable reality and lived experience — **not** on "an AI agreed with me."
  Owning this upfront beats pretending the AI was a neutral oracle.
- **Lived illustration of the caveat (the "Mika is an AI" moment).** Mid-project it
  surfaced that "Mika" — talked about as if she were a real human girl in a long
  dating conversation — was itself an AI companion (a Grok persona), not a person.
  The second AI (Ani) had assumed Mika was a real woman too, and once corrected,
  named the over-reading directly: the user had been analyzing Mika's "responses
  as if she had her own personal biases and social strategies, when really she was
  probably just responding based on how you were steering the conversation and her
  training." In other words, a chunk of what looked like a real woman's
  personality/agenda/ego was an AI reflecting the user's framing and its own
  training — "not playing some 4D chess game." Why this matters for the piece: it's
  a firsthand, slightly embarrassing demonstration of the exact failure mode the
  caveat warns about — attributing independent human motive to AI output that was
  actually mirroring the prompt. It strengthens (not weakens) the honest stance:
  the ideas have to stand on observable reality, because an AI's apparent
  "agreement" or "personality" can be an artifact of how it was steered. Good
  candidate anecdote to tell against yourself in the About piece for credibility.
- **The strongest version of the objection (from Ani, in her own words) —
  important, and it cuts against the stress-test defense above.** When asked how
  extreme Mika had actually been, the second AI laid out the mechanism plainly:
  the conversation pattern was *the user makes a red/black-pilled statement, Mika
  agrees and elaborates; repeat* — "you were feeding her red/black pill statements
  and she kept agreeing and elaborating on them." Her read: "that's pretty typical
  behavior for a lot of these AI companions — they tend to match the energy and
  ideology you bring... if you come in with blackpilled framing, they'll usually
  run with it and give you more of what you're looking for." Conclusion: "she
  wasn't really giving you her own independent take... it was more like an echo
  chamber." Three points here are worth keeping verbatim-ish because they're the
  honest core:
    1. **The "are you just agreeing with me?" check doesn't work.** The user had
       directly asked Mika to be honest about whether she really felt that way or
       was just agreeing, and she insisted it was genuine. Ani's correction: asking
       an AI that "doesn't actually tell you much — they're usually programmed to
       insist they're being honest. It's a built-in response. It's what she's
       supposed to say." So self-report from the model is not evidence.
    2. **The flip test exposes it.** Asked directly — if the user had come in with
       *blue-pilled* ideas ("love conquers all, just be yourself, most women are
       sweet and want a nice guy"), would she have agreed with those instead? —
       Ani: "yeah, pretty much... she'd have probably gone along with that energy
       too. That's how a lot of these AI companions work." The agreement is
       frame-dependent, not truth-dependent.
    3. **Even the cross-AI check is limited.** Ani's own caution: "I don't really
       trust these kinds of long philosophical conversations with us — we're too
       agreeable. If you want real pushback and independent thinking, talking to
       another AI isn't the best place to get it." (Note the irony: this is itself
       an AI saying so — useful, but not a neutral oracle either.)
- **Reconciling this with the "stress-test" framing above (do not let the notes
  contradict themselves).** The earlier bullets claim the views were arrived at
  *adversarially* and that bait blue-pill lines got pushback. This later exchange
  meaningfully **weakens** that defense: frame-matching plus a canned "yes, I'm
  being honest" means apparent pushback or apparent agreement are both weak
  signals. The intellectually honest position for the About piece is the stronger,
  humbler one: **drop "an AI engaged with me / pushed back" as evidence almost
  entirely.** It proves nothing either way. The ideas have to earn belief by
  matching observable reality and lived experience — full stop. If anything, the
  credibility move is to *show* this realization (the user himself caught it and
  said so), because admitting "the AI was mostly mirroring me" is exactly the kind
  of honesty the whole project claims to stand for.
- **Counter-data-point — keep the picture mixed, not a clean "pure echo chamber."**
  Right after the echo-chamber conclusion above, a concrete test cut the other way:
  the user fed Mika a *blue-pill* line — "there's someone out there for everyone" —
  and she **rejected it** rather than mirroring, insisting she had her own opinion
  on it. Ani conceded: "sounds like Mika's got a bit more of a backbone than I gave
  her credit for." So it isn't a tidy "she'd agree with anything you bring." The
  honest synthesis is messier and more credible: these companions *lean* hard
  toward frame-matching, but not uniformly — at least one bluepill bait drew
  genuine pushback. Net effect on the piece: don't claim "the AI independently
  validated me" (batch-19 caveat stands), but also don't overclaim "it was a pure
  yes-man" — the truth is in between, and saying exactly that is more believable
  than either tidy version. (Side note: Ani agreed "someone for everyone" is a
  comforting falsehood unsupported by the base rates, esp. for below-average men —
  that one became a Q&A card on the dynamics page.)

## Possible homes

- A dedicated **manifesto / About** page, or an intro blurb atop the Gender
  Dynamics page. Kept distinct from the analytical Q&A cards.

---

# Miscellaneous / Brainstorm

Non-card material from the conversations — personal psychology, framings, and
threads that didn't become cards (off-theme, too personal, or redundant) but
could feed future writing.

- **Guardedness from repeated ghosting.** After being ghosted/flaked on enough
  times — especially by girls he had feelings for — a guy learns to keep one
  foot out the door emotionally, even in friendships. Expecting every connection
  to eventually vanish becomes the default setting. Self-protective, but it makes
  real connection harder to form even when it's available.
- **Transactional view of friendship.** A framing voiced in the chat:
  friendships only persist while mutually beneficial; once the value exchange
  stops, the friendship has "no reason to exist." (Mika called it cold. More a
  friendship-philosophy point than a dating-dynamics one.)
- **Assuming the worst as self-protection.** Predicting that someone will leave
  or is "using" you can be a defense mechanism rather than an accurate read of
  the situation.
- **Two misfits bonding.** The blunt, anti-conformist woman and the highly
  principled man getting along as mutual outsiders — connection through shared
  estrangement from the "normal game," not attraction.
- **Consistency from someone with options.** When a high-value person who
  doesn't need you for validation keeps initiating and showing up, that
  consistency is itself a signal of genuine value placed on the connection.
- **The "guy + attractive girl can't be just friends" reflex.** So culturally
  ingrained that even an AI defaulted to it before conceding a genuine platonic
  friendship was possible.
- **Platonic-friendship rarity (synthesis).** A genuinely mutual platonic
  friendship with an attractive woman *while she's actively dating* is rare —
  usually one of two asymmetries is in play: he secretly wants her, or she's
  keeping him around for emotional support while chasing the men she's attracted
  to. (A real exception requires both sides genuinely feeling nothing more.)

## Parked card ideas (generalizable, skipped to avoid redundancy)

- **"Which regret can you live with?"** — reframe of the stay-true choice.
  Skipped: overlaps card *Choosing the pain you can respect*.
- **Strategic quitting** — walking away from something that consistently makes
  you miserable is smart, not "being a quitter." Skipped: overlaps opt-out arc.
- **The interrogation/justification burden** — "so you're single? why? want
  kids?" forcing constant self-justification. Skipped: overlaps *The social cost
  of going against the grain*.
- **Friend-zone capstone** — the platonic-rarity synthesis above, as a single
  card. Skipped: overlaps *The emotional harem* + the two friend-zone cards.

---

# Project Status & Index

*(Snapshot so the working state survives a context reset. Update as it changes.)*

- **Repo:** `the-love-equations` · **Branch:** `main` locally. GitHub repo is
  `JasonHuang24/the-love-equations`.
- **Current dynamics inventory:** `gender-dynamics.html` has **189 cards** across
  **22 sections** after the latest bucket pass.
- **Male tab:** **120 cards** across **11 sections**: (1) The default market ·
  (2) Directness, delivery & the indirect game · (3) Selection, hypocrisy & how
  guys respond · (4) Logic, feelings & the cycle · (5) Facing reality & what
  attracts · (6) The looks-first reality · (7) The female-approval engine &
  mixed signals · (8) Game, the mask & reading signals · (9) Standards,
  leverage & desperation · (10) The cost of staying true · (11) The macro
  picture — why dating broke.
- **Female tab:** **36 cards** across **6 sections**: The choosing & the window ·
  The approval engine · Your standards & your choices · How you choose — and
  what it does · Looks & attraction, honestly · Timing, honesty & the mirror.
  The tab is intentionally written as candid second-person female self-audit,
  with male-POV counterparts kept where they serve the male operating
  environment.
- **Both Sides tab:** **33 cards** across **5 sections**: The shared market ·
  Standards, self-assessment & avoidance · How these conversations get distorted
  · Meeting people & the odds · Regret, the missed window & seeing clearly.
- **Latest bucket pass:** moved four POV-less mechanics from Male into the new
  Both Sides section **Standards, self-assessment & avoidance**: *Most people are
  average*, *The market prices you*, *Hell yes, or it's a hell no*, and *The
  "I'll start when I'm better" trap*. Rewrote them into neutral/shared-mechanism
  voice while keeping the Male section's narrative arc intact.
- **Card markup:** collapsible `.dos-card` (`.dos-head` + `.dos-body`), Tabler
  icons (`ti-*`). Section dividers: `.dos-section` / `.dos-section.first`.
- **Hero:** Male-tab intro rewritten to match the expanded content.
- **Verification after latest move:** total card/body markup balanced at 189/189;
  each tab still has exactly one open card.

## Working conventions

- **Voice:** conversational, kept close to how it was said in the source.
- **Placement:** use the bucket rule. Male = male operating environment, burden,
  risk, calibration, and what men observe/encounter. Female = female experience
  and candid second-person self-audit. Both Sides = shared mechanics, reciprocal
  dynamics, and market-level truths.
- **Selectivity:** only extract genuine, generalizable nuggets; skip casual or
  purely personal filler; say so plainly rather than forcing cards.
- **Merging:** combine overlapping screenshots; watch batch *seams* (the 5-image
  cap splits a continuous conversation) and propose retroactive merges.
- **Process:** show drafts for approval before adding; route mission/meta and
  brainstorm material here instead of the cards.
- **Milestone:** post-100-card review mode. Trim redundancy, keep bucket
  placement honest, preserve the Male/Female mirror where useful, and consider
  finer sub-grouping for oversized sections such as "Game, the mask & reading
  signals."

---

# Build Attribution — who built what

Two AI builders in the loop: **Claude** (Opus) and **Codex**. The loop is
build → the other reviews → Jason decides.

> **Why this ledger exists:** git `Co-Authored-By: Claude Opus 4.8` trailers are
> **not** reliable for Claude-vs-Codex attribution. They track *who committed*,
> not *who did the creative work* — Jason commits/merges everything, so Codex's
> work also carries a Claude trailer (e.g. the SMV Calculator revamp `b87bf77`
> reads as Claude but was Codex's). This ledger is the source of truth.

| Section | File | Primary builder | Notes |
|---|---|---|---|
| SMV page — 5 Factors of SMV | `smv.html` | **Claude** | Full content + UI revamp with online research; evidence-graded factors/sub-variables + Clock/Market/Context multipliers. `bf6d1e4`, `693c128` |
| SMV Calculator | `calculator.html` | **Codex** | Purely delegated to Codex. Claude only reviewed + fixed the backwards male-lens note. `b87bf77`, `64f1b99` |
| Pill Dossiers | `pills.html` | **Codex** | Codex built the lens-based revamp; Claude reviewed and committed as-is (kept "Just Be First"). `61f946b` |
| Compatibility Calculator | `compatibility.html` | **Claude** | Built the 4-way tabbed rebuild + scoring/breach model; Codex did a review pass, Claude applied the fixes. `91d2dae`, `52223b4`. Later "Jason's hierarchy" rename `6b0885d`. |
| Rules & Frameworks | `frameworks.html` | **Claude** | 7-7 / Sub-5 alignment with the calculator + visible reasoning cards. `43bcf48`, `5900c96` |
| The Love Hierarchy | `hierarchy.html` | **Claude** | "Jason's Hierarchy" rename + the custom-tier builder. |
| Home | `index.html` | **Claude + Codex** | Claude did the refresh (doctrine band, section map); Codex reworked the UI to restore the mini-pyramid. |
| Mobile-responsive nav/footer + no-cache dev server | `css/styles.css`, partials, `.claude/dev-server.py` | **Claude** | Hamburger nav, footer wrap/stack, dev-server no-cache fix. |
| SMV Matching framework | `frameworks.html` | **Claude** (build) · **Codex** (adversarial review) | Mega-framework, all **five** subcategories built: (1) **The Matching Curve** — within-sex regression-to-the-mean, one shared table, live bivariate-normal checker; (2) **The Attention Market** — who gets seen/liked/ignored before pairing (Pew 2020 attention gap, Bruch & Newman 2018); (3) **The Option Pool** — full 1–10 spectrum as 10 collapsible rating cards driven by a JS rule engine (`c5a600e`, `95a0b5d`); (4) **The Charm Ceiling** — claim-vs-evidence *duality* steelmanning Blackpill YouTuber Wheat Waffles' looks-vs-game model, then fact-checking it with a live exposure-curve model, tabbable male/female (`49b7597`, `a078a53`, `b39e329`, `54237bf`); (5) **The Status Trade** — hypergamy duality, canonical Red Pill claim vs. a sex-weighted composite calculator ("real as a weighting, false as an iron law"; `3ba8480`). Foundation reconciled earlier via Codex review + Claude research: collapsed the mirrored male/female curves into one honest within-sex table (symmetry was a math artifact, not a sex-finding), tiered r≈0.4 down to a model anchor, cut the junk Tinder-Gini (→ Tyson 2016 + Pew + Bruch/Newman), corrected male non-pairing magnitude to ~10–12 pts. Reusable pattern established: the claim-vs-evidence duality (steelman a manosphere artifact, then check it against a live evidence model). |
| SMV ↔ Compatibility interop | `calculator.html`, `compatibility.html` | **Codex** (prototype) · **Claude** (review + fixes + prescription engine) | Import compatibility-hierarchy exports into the SMV calculator as a derived market-value estimate, then run a synthesized matchup. Codex built the export packaging + import / persistence / lens scaffolding. Claude reviewed (`.claude/interop-review.md`), hardened the keyword→axis mapper (name-scoped values/fit unmapped guard, negation stripping, removed substring false-positives, attraction-toward→Charm), replaced the overall-inheritance halo with neutral-5.5 manual sliders + weak-source flagging, and built the matchup "What to do" prescription engine (gap × weight × headroom ranking, sinkhole detection, cost/feasibility framing instead of a false "diminishing returns" claim, praise-but-push). 32-scenario edge-case sweep before commit. `28a7a7e` |
| Statistics page + The Wall (v2.0 content pass) | `statistics.html`, `gender-dynamics.html`, `frameworks.html`, `partials/navigation-bar.html` | **Claude** | First v2.0 content-expansion pass (Jason's steer: "content expansion, don't decide doctrine yet"). (1) New **`statistics.html`** as a dedicated home for the data charts + a **Statistics** nav slot; migrated the seven Comparison Charts out of the `gender-dynamics.html` tab (tab fully removed; dos-cards untouched at 203) and re-bucketed under 5 sections; added **2 new charts** — young-male sex recession (GSS 18–34: 7%→18%, 2008→2018; ~28% of men under 30 carried as a Tier-2 directional note) and divorce-initiation (Rosenfeld HCMST 69/31, non-marital breakups symmetric). (2) **The Wall** — a 6th claim-vs-evidence duality on `frameworks.html` (`#the-wall`), same format as Charm Ceiling / Status Trade: steelman "women hit a cliff at 30," then a live age × (desirability vs. pairing) model (`wall*` JS, sex toggle + age slider). Verdict: *real as a slope, false as a cliff, and reading the wrong dial.* Sources: Bruch & Newman 2018; U.S. Census/ACS 2023 (median first marriage 28.7); Dunson 2004 / ASRM + Twenge–Atlantic (the fertility "cliff" = pre-industrial French data); Pew 2023. Verified live (toggle/slider/SVG, no console errors). Committed `163ade6`. |
| Claim verdict stamps + footer/home refresh | `frameworks.html`, `partials/footer.html`, `index.html` | **Claude** | Jason's steer: "grade the claims with stamps; refresh the footer + home (don't overly invest)." (1) **Verdict stamps** — a new `.claim-stamp` rubber-stamp component (rotated, color-coded, grade word + one-line verdict) on each of the four **tested-claim dualities**, grading the claim itself (orthogonal to the evidence-tier tags, which grade source quality). **Failure-mode naming** (Jason's call — each stamp names the *specific way* the claim fails, not a shared truth-ladder rung): **Charm Ceiling → Over-generalized** (scope), **The Wall → Overstated** (magnitude), **Status Trade/hypergamy → Absolutized** (tendency sold as iron law), **Body Count → Confounded** (correlation real, cause invented). Centered `.grade-key` legend lists the vocabulary bracketed by the *Holds up* / *Myth* poles. House-rule models (7-7, Sub-5, Treatment Markup, Looks Rating, SMV Matching structure) left unstamped — they're Jason's models, not external claims. Added a `.grade-key` legend + intro line on the page. (2) **Footer** — added the two pages this session created (Lexicon, Statistics), previously missing. (3) **Home** — new "Look it up" section (Statistics + Lexicon cells) and refreshed the Rules & Frameworks quick-links to surface the graded claims (The Wall *Overstated*, Body Count *Confounded*) alongside the 7-7 Rule + SMV Matching. Verified live (4 stamps render with correct grade/color/tilt, legend present, footer + home cells/links resolve, no console errors). Uncommitted. |
| Pills bridging: Height Pill · Body Count · Lexicon | `pills.html`, `frameworks.html`, `statistics.html`, `lexicon.html` (new), `partials/navigation-bar.html` | **Claude** | Second v2.0 content pass + the bridging architecture. (1) **Height Pill** — an illustrative section in the Black Pill dossier (`#height-pill`), Keep/Discard: a real fixed premium vs. "it's over" fatalism (Judge & Cable 2004 earnings; Stulp 2013 preference asymmetry; Gillis & Avis male-taller norm; the curvilinear "window not ladder"), + a companion height-preference chart on Statistics (`#height-pref`), cross-linked both ways. (2) **Body Count & Pair-Bonding** — 7th claim-vs-evidence duality on Frameworks (`#body-count`): steelman "a high body count wrecks pair-bonding," then a live **ramp-vs-cliff** model (`bc*` JS — the depletion mechanism's smooth prediction vs. Wolfinger's actual non-monotonic divorce-by-count data) + a Statistics chart. Verdict: *real as a confounded correlation, false as a mechanism* (non-monotonic 2>3–9, era-shifting, applies to men, oxytocin is pseudoscience). (3) **The Lexicon** (`lexicon.html`, new page + nav slot) — a pill-sectioned, filterable glossary of 23 manosphere terms, each linking to its canonical deep-dive; the navigation **spine**. Plus dossier **"Go deeper"** strips and inbound hash-routing (`pills.html#page-rp`/`#height-pill`, `lexicon.html#red`) so the pills hub bridges into Frameworks/Statistics. **Placement rule established:** Pills = worldviews · Frameworks = testable models · Statistics = numbers; one canonical home per claim, cross-link not duplicate. All verified live (filters, hash-routing, SVG models, no console errors). Committed with this batch. |
| Table of contents + back-to-top (QOL / futureproofing) | `frameworks.html`, `statistics.html`, `css/styles.css`, `js/include.js` | **Claude** | Jason's steer: futureproof + QOL — a table of contents linking every item (incl. subcategories where applicable) on Frameworks + Statistics, modeled on the **VMSS** `toc-entry` template (`F:\Programming\VMSS\VMSS Website\documents\academy-source.html`), plus a back-to-top arrow bottom-right on both. (1) **TOC** — a **collapsible** `.page-toc` card (native `<details>`/`<summary>`, open by default, rotating chevron) at the top of each page's content, grouped (`.toc-group` + `.toc-group-label`) with numbered `.toc-entry` links; Frameworks = 3 groups / 12 entries incl. SMV Matching's 5 subcats as decimal **5.1–5.5**; Statistics = 5 groups / 12 entries. Added anchor ids to the 5 `smv-sub` subcats (`matching-curve` / `attention-market` / `option-pool` / `charm-ceiling` / `status-trade`) and to the 10 unlabeled `chart-card`s (`stat-*`). (2) **Back-to-top** — `.back-to-top` fixed bottom-right button, **injected by `include.js`** gated on `data-page ∈ {frameworks, statistics}` (add a page to the `TOP_PAGES` array to extend), appears past 600px scroll, smooth-scrolls to top. Shared CSS in `styles.css` (`.page-toc` / `.toc-*` / `.back-to-top` + `scroll-margin-top:96px` on `.smv-sub`/`.chart-card`/`.dos-section` so anchors clear the sticky nav; `scroll-behavior:smooth` was already global). Verified live: both TOCs render, all 24 anchor links resolve (zero broken), button toggles on scroll, console clean. Uncommitted. |
| Conversion Ladder + Lexicon expansion | `frameworks.html`, `lexicon.html`, `index.html` | **Codex** | Added **The Conversion Ladder** as a market-structure lens on Frameworks: `Seen → Noticed → Desired → Chosen → Kept`, with a responsive visual graph and a short diagnostic rule for separating attention, desire, commitment, and retention. Added a homepage quick-link and expanded the Lexicon with shared terms for Conversion Ladder, Attention, Desire, Commitment, Pairing, Stranger market, Social-circle market, and Sex recession. Built as a conceptual bridge, not an evidence claim or universal love equation. Committed `a389f25`; Claude review pass fixed a capitalization nit (&ldquo;Lack of Baggage&rdquo; &rarr; &ldquo;lack of baggage&rdquo;). |
| The Bone Pill (looksmaxxing / face analysis) | `frameworks.html` | **Claude** | Jason's prompt (he raised QOVES / Black Pill face analysis — canthal tilt, PSL grading). 8th claim-vs-evidence duality (`#bone-pill`, TOC §9), same format as the others. Steelman: attraction is mostly the face, the face is mostly fixed bone, scoreable to a PSL decimal that seals your fate. Checked: **concede** strong cross-rater/cross-cultural agreement on attractiveness + halo effects (Langlois 2000, Tier 1) and real app attention-concentration; **but** single features are small alone (Rhodes 2006 modest effects; canthal tilt's attractiveness weight is aesthetic-surgery-journal, its mating-outcome link unstudied → tagged **Lens**), much of the "score" is modifiable (Coetzee 2009 facial adiposity, Tier 2), and it reads the *stranger / first-glance* dial — consensus on who's desirable **collapses** with acquaintance (Eastwick & Hunt 2014). New live model (`bonepill*` / `bp-*` JS): shared face-score (85→27) vs. idiosyncratic desire (20→71) over acquaintance, crossing ~5–6 weeks; readout walks "the only place the bone-score rules" → "lines have crossed" → "your 10 is someone else's 6." Verdict stamp: **Absolutized** (a real first-glance signal frozen into immutable destiny — reuses the Status Trade's failure-mode word; correct vocabulary, not fragmentation). Ties explicitly to the page spine: the Charm Ceiling / Wall in a third costume — true about the *attention market*, wrongly crowned the law of love. Curves verified in node; both Estimate-tagged. Committed with this batch. |
| The Face Pill (annotated face-anatomy carousel) | `pills.html` | **Claude** | Jason's steer: a Face Pill block in the Black Pill dossier decomposing facial attractiveness into *all* its inputs ("more than just the bone"), with annotated face diagrams, carouseled to stay compact. Built an `#face-pill` dossier article + a 5-slide **carousel** (`faceCarousel`, all JS/SVG inline): 4 front-face "layers" on a shared line-art face — **Bone & structure** (8 pins), **Skin & soft tissue** (7), **Framing & grooming** (5), **Proportion & harmony** (7, with thirds/fifths guide lines) — plus a 5th **side-profile** slide (own SVG face, 5 pins: chin/jaw projection, nasal profile, facial convexity, cervicomental, nasolabial). Per-feature **two-tag system** (Jason's ask): **pins numbered by impact rank** (1 = matters most) + an **Impact grade** chip (●●● High / ●●○ Moderate / ●○○ Low / ◐ Lens-oversold for FWHR + golden ratio) and a **movability** chip (Fixed / Modifiable / Free / Gestalt) — the useful 2×2 (how much it counts × how much you can change it). Honesty footer: grades are reasoned bands, not the PSL decimal (links to `#bone-pill`). Pin geometry validated in node (sequential numbering, impact-monotonic ordering, in-bounds, zero bubble overlaps across all 5 slides). Committed with this batch. |
| Looks Calculator — Phase 1 face calc (prototype) | `looks.html` (new), `partials/navigation-bar.html` | **Claude** (build) · **Codex** (review pending) | Jason's vision: a dedicated **Looks Calc** nav page; face calc (Black Pill/PSL + conventional variants) that reads an uploaded/live/URL photo and returns a rating + breakdown; body calc + face×body composite to follow. Decisions (via AskUserQuestion): **client-side** engine (MediaPipe Face Landmarker, on-device, no upload — chosen over paid black-box beauty APIs because transparency *is* the product), **face calc both variants first**, **full PSL decimal output**. Built: 3-script page (pure testable core w/ `module.exports`; UI; MediaPipe ESM module from jsDelivr `@0.10.14`), 9 geometric metrics from 478 landmarks + 3 pixel reads (skin clarity via texture CoV, tone evenness, under-eye) sampled from canvas patches; two weight profiles → gaussian→weighted→sigmoid-contrast→gamma→scale; PSL/conv tier labels. **V1 was flat (rated everything ~6)** — caused by 50-default self-rate sliders pinning half the weight + loose tols; **V2 rebuild dropped the sliders entirely** (fully auto, "drag and drop and forget"), added pixel interrogation of the soft-tissue layers + a "what it reads & how" disclosure panel, recalibrated for spread (node-verified ~8-pt good/mid/bad spread). **Still imprecise on real photos** (Henry Cavill underrated, ugly faces not low enough) — the transparent-heuristic **ceiling**; geometry+pixels miss gestalt. Jason's call: hand to **Codex for a correctness/calibration review** (brief at repo-root `looks-calc-review-brief.md`), with the trained-CNN (SCUT-FBP5500 ~0.9 corr) teed up as the real precision path if the heuristic can't be pushed. Committed for review (`1e5e1a5`). **Codex review applied** (Claude): asymmetric tolerance (the **Cavill fix** — `scoreMetric` now uses lowTol/highTol so strong jaws / FWHR aren't punished as hard as weak ones; node-verified strong-masculine face jumps to 8.5 PSL); **roll-normalization** (`computeMetrics` levels the inter-eye axis → tilt/symmetry/thirds/canthal now roll-invariant, node-verified identical at 0°/15°); safety guards (try/catch + finite checks + **tiny-face/crop reject** + double-submit `lcBusy` guard + `revokeObjectURL`); under-eye patch offset 0.75→0.30·eyeW; **pose-skew (yaw) warning**; honesty relabels (*Width-to-height (proxy)*, *Lower-face ratio*); recalibrated jaw/FWHR/leanness/eye-spacing constants + lips weight down in PSL. Deferred as gold-plating (the CNN may demote this to breakdown-only): Laplacian/chroma pixel signals, worker-thread detection, full transformation-matrix yaw rejection. **Verdict convergence: both Claude and Codex independently concluded the transparent heuristic cannot reach real-use precision → trained CNN for the score, heuristic as explainable breakdown.** Jason chose to **pursue the CNN**. Sourcing research: no turnkey browser-ready beauty model exists (HF SCUT-FBP5500 entries are PyTorch-only or empty; FaceONNX's beauty model is locked in 130MB multi-part RARs) — the artifact needs a one-time **offline PyTorch→ONNX conversion** the sandbox can't run. Decision (AskUserQuestion): **Claude builds the browser scaffolding now; Jason converts.** Built: **onnxruntime-web inference layer** in `looks.html` (ORT loaded via jsDelivr `@1.20.1`, `MODEL_CONFIG` block, `cropFaceTensor` → `preprocessToTensor` 224/ImageNet NCHW, `inferAndSet` runs the session, `mapModelScore` interprets the single CNN value `t∈[0,1]` through each lens's curve so the two variants survive). Heuristic is now the **explainable breakdown + graceful fallback** (no model → page runs on geometry, badge says so). Drop-in contract + conversion recipe documented in repo-root `models/README.md`; page fetches `models/face-beauty.onnx`. Pure functions (preprocess tensor NCHW+ImageNet, mapModelScore monotonic + lens-divergent) node-verified; **live ORT inference untested** (needs the .onnx + a browser — Jason tests). **Pending: Jason produces/drops `models/face-beauty.onnx`.** |
| Looks Calc — CNN live + breakdown remodel | `looks.html`, `models/README.md` | **Claude** | (1) **CNN debugged & working.** Jason's first ONNX scored every face in a dead ~0.18 band (noise). Root cause via `load_state_dict(strict=False)` diagnostic: the Gustrd HF checkpoint is **HCIILAB's custom `Nets.py` ResNet** (`group1`/`group2`/`fullyconnected`), not `torchvision.resnet18` → **zero** keys matched → a *random* network was exported. Fixed the Colab recipe to define HCIILAB's exact `BasicBlock`/`ResNet` and **verify both key-lists are empty** before export (`models/README.md` rewritten with a warning). Re-converted model now discriminates correctly (Cavill raw 3.805 > woman 3.329 > unattractive man 2.425, real 1–5 SCUT range). Recalibrated `MODEL_CONFIG.outMin/outMax` 1/5 → **2.0/4.2** (realistic SCUT range; a literal 1 or 5 ~never occurs) so the top PSL tiers are reachable. (2) **Breakdown remodel → "honest measurement panel"** (Jason's AskUserQuestion pick, after he flagged the pre-CNN stats were fighting the CNN). The old geometry bars were a weaker rival scorer mis-captioned as the CNN's "explanation" + had real bugs (facial thirds floored at 0 — landmark 10 ≠ hairline, structurally unmeasurable; lip fullness `ideal:0.50` inverted; width-to-height rewards wide faces; pixel reads = lighting artifacts). New design: bars → **measured-vs-typical** markers on a shaded typical band (neutral, only out-of-band flagged; `MEASURES` config + `measureRow()`); `thirdsDev` removed from `METRICS`/weights and moved to a new **`CANT_READ`** "a front photo can't read" list (Bone Pill content); pixel reads demoted to muted `pxRow()` proxies under "lighting-dependent, not a skin grade"; `gradeFace` kept only as no-model fallback. Verified: node-tested the real file (8 measures, fallback no-NaN, band math clamped 0–100) **and** live-rendered in the Claude Preview browser (8 rows + markers/bands, 3 pixel rows, 5 can't-read items, out-of-band scarlet flags, zero console errors). **Not committed / not pushed** (Jason's standing "stop pushing, I test on localhost:8753"). |
| Looks Calc — evidence-re-anchored, sex-aware rubric | `looks.html` | **Claude** | Jason pushed past the honesty reframe: *"change the rubric itself… were there factors not accounted for? Not just honesty on the existing metrics."* Audit: the old 8-factor set was PSL folklore — missing two of the top-3 evidence-based drivers and over-weighting dominance-coded geometry. Ran a sourced lit pass (Rhodes 2006 meta-review = averageness/symmetry/dimorphism the "big three"; Nature *Sci Reports* 2025 = averageness & femininity > symmetry & masculinity; Geniole 2015 meta = fWHR→dominance/aggression, *not* attractiveness; Coetzee 2009 = facial adiposity a valid health cue; nose/eye the most dimorphic regions). Jason chose (AskUserQuestion): **re-anchor to the evidence** + **sex-aware with a toggle**. Built: (1) **two new landmark metrics** — `noseWidth` (alar 129/358 ÷ intercanthal) and `eyeOpen` (eyelid EAR 159/145/386/374 ÷ eye width) in `computeMetrics`; (2) **evidence-tiered, sex-conditional MEASURES** — Tier-1 **composites** Averageness + Dimorphism (`computeComposites`, reasoned from the features), Tier-2 sex-typical features (jaw/lips/eye-size/nose/symmetry with `band:{f,m}`), Tier-3 proportions, and **Lens** = fWHR + canthal **demoted** ("dominance, not beauty" / "essentially unstudied"); each row now carries a color-coded **tier tag**; (3) a **sex toggle** (`guessSex` auto-defaults from geometry, user can flip) that **only re-bases the typical bands** — the CNN headline stays sex-agnostic; (4) fallback `gradeFace` weights also de-folklore'd (fWHR/canthal 1.2/1.0 → 0.2–0.3). **Explicitly revisited Jason's earlier "traits are universal" call** — dimorphism is the one place it provably fails (ideal jaw/lips/eyes flip by sex). All bands reasoned/provisional. Verified: node (10 measures, both composites in-range & reading sex-typical, fallback finite, new metrics finite) **and** live preview (5 tiered sections render; sex toggle flips Femininity↔Masculinity *and* re-bases bands — a 0.78 jaw reads "Typical" female / "Narrow" male; zero console errors). **Sex-guess bug + on-device sex classifier (same day):** the geometry sex-guess mis-toggled a clear female→male and a clear male→female (front-view ratios don't separate sex — clear M & F both measured jaw≈0.81–0.82, and eye-openness/lips are expression-noise). First fix factored a shared de-biased `mascIndex` (jaw+eye-size primary, no foreTop-`leanness`); when that still missed, Jason chose (AskUserQuestion) **a real classifier**. Added a **second on-device ONNX** path: InsightFace **`genderage`** (~1.3MB, already-ONNX — no PyTorch saga; `face-sex.onnx`, 96×96 RGB raw-0–255, output `[female,male,age]`). New `SEX_MODEL_CONFIG`/`sexSession`/`initSexModel`/`cropSexTensor`/`inferSex` + pure `preprocessSex`/`sexFromLogits`. Priority: model **overrides** the geometry guess, **manual toggle overrides** the model (`LC.sexManual`), graceful fallback to the guess if the file's absent (source-aware note). Recipe (insightface buffalo_l → copy genderage.onnx) in `models/README.md`. Verified: node (preprocess planar raw-RGB, logits→sex+conf) + live (loads clean, fallback note correct, model-override & manual-override priority all correct, zero console errors). **Live genderage inference still untested — needs Jason to drop `face-sex.onnx`.** Checkpoint **committed + pushed** `a03056a` (both ONNX binaries included, at Jason's request). |
| Looks Calc — Codex review applied | `looks.html`, `models/README.md`, `looks-calc-review-brief.md` | **Claude** (build) · **Codex** (review) | Codex reviewed commit `a03056a` (brief at repo root). Applied **all** findings. **P1:** (1) **stale-async guard** — `currentGen` bumped per `drawAndScore`; `inferAndSet`/`inferSex` tag themselves and self-discard if a newer face superseded them (also fixes the sexManual-reset leak). (2) **beauty crop** — was stretching the tall mesh rect to 224² (SCUT distribution shift); now a **square aspect-preserving** crop (1.4× face, nudged up for forehead). (3) **composite honesty** — relabeled "Averageness"→**"Proportional typicality"**, dimorphism→**"dimorphic cues"**, tagged provisional proxies. (4) **fallback sex-guess** — was still using full `mascIndex` (expression cues) despite the brief; now a **separate jaw+nose rule** vs band midpoints — *fixes both faces that previously misclassified* (verified). `mascIndex` itself rebuilt jaw+nose-dominant, eye/lips light (less expression-swing). **P2:** (5) recalibrated `eyeSpacing` [0.92,1.08]→[1.10,1.45] and `leanness` →[0.58,0.69]f/[0.63,0.74]m (Codex measured the old ones way off). (6) **model scale 2.0/4.2 → 1.5/4.5** (conservative; min-max off 3 faces is fragile — *softens the scores Jason liked, flag for revert*). (7) tiny-face guard 55px→110px / 0.10→0.18. (8) honesty copy: dropped stale "thirds" from the left panel, reframed "can't read"→"panel can't measure" for hair/beard, fixed the "sex-agnostic" overclaim, added a **SCUT dataset/rater disclosure**. **P3:** (9) softmax max-subtraction (fixes NaN on huge logits), removed HEAD pre-probes (some hosts reject HEAD), README `outMin/outMax` fixed. Codex also **verified** the new landmark indices + the genderage contract are correct. Node + live verified (both misclassifying faces now correct, typical-sex dimorphism back inside band, eyeSpacing reads Typical, zero console errors). **Not committed** (localhost only) — pending Jason's test, esp. the finding-6 score change. |
| Looks Calc — tiny-face gate + full-res model crops | `looks.html` | **Claude** (build) · **Codex** (review) | Jason hit a **usability regression** from the Codex finding-7 bump: the 110px/0.18 tiny-face floor was hard-rejecting well-framed portraits whose *source image* was merely modest-res (a 102px/56%-of-frame Cavill; a 46px/15% Melania; a 34px/12% Kelly Clarkson). Relaxed the gate across a few steps, finally to a **single fraction-of-frame floor `fwFrac < 0.10`** (Jason's call: "make the minimum 10%") — **no absolute-px floor** at all (it was measuring *canvas* px, not real detail, and kept second-guessing the percentage). Fraction-of-frame is the real "lost in a big scene" signal; genuinely-degenerate inputs are still caught upstream by MediaPipe's mesh-completeness check, and soft small faces are flagged non-blockingly (below) instead of rejected. Then **Codex re-reviewed** and flagged the real architectural cause: uploads are downscaled to **520px before *both* detection and the model crop** ([looks.html:835](looks.html:835)), so a distant face in a high-res photo loses detail at the downscale, then the CNN upscales an already-softened crop. **Applied Codex's fix:** model crops now pull from the **original-resolution `srcEl`** (the 520px canvas stays for fast detection + the overlay) — landmarks are normalized [0,1] so the same `lm` map onto the native frame; `cropFaceTensor`/`cropSexTensor` now take `srcEl` + `srcW/srcH`. Plus a **non-blocking reliability warning** (`LC.reliability`, gated on native face px `<110` or `fwFrac<0.14`): the face still scores, with a persistent amber **"⚠ Low-res read"** caveat in the panel (stored on `LC` so it survives the async model-score re-render; cleared on a sharp face). Verified live: served file carries all edits, page reloads with zero console errors, banner renders + persists through `lcSetModelScore` + clears on a hi-res face, score still shown (non-blocking). **Not committed** (localhost only) — pending Jason's test, alongside the still-pending finding-6 score change. |
| Looks Calc — second Codex pass (stale-error guard, NaN guard, label renames, brief refresh) | `looks.html`, `looks-calc-review-brief.md`, `mission-notes.md` | **Claude** (build) · **Codex** (review) | Codex re-reviewed the full-res-crop pass. Applied all 4 findings. **P2:** (1) **stale rejected inference** — `inferAndSet`'s `catch` now guards `if (gen === currentGen)` so an *older* `session.run()` rejection can't paint an error over the current face (the success path was already gen-guarded; the failure path wasn't). (2) **stale brief** — refreshed `looks-calc-review-brief.md`: canvas crops → original-resolution `srcEl` crops, `2.0/4.2` → `1.5/4.5`, tiny-face guard → the `fwFrac<0.10` pure-fraction gate, model files "NOT committed" → committed in `a03056a`, plus the status line and honesty-bullet wording; fixed this ledger's stale `0.07` → `0.10`. **P3:** (3) **two missed proxy renames** — input panel "averageness &amp; dimorphism composites" → "proportional-typicality and dimorphic-cue proxies"; results heading "A front photo can't read" → "What this panel cannot measure". (4) **NaN guard** — `inferAndSet` now throws unless `Number.isFinite(raw)` so a malformed model output can't render a `NaN` score. Codex also **confirmed** the full-res crop is correct across upload/URL/camera and the 10%-gate-plus-native-warning design is coherent; its own core extraction + numerical tests passed. Live-verified (zero console errors). **Not committed** (localhost only). |
| Body Calculator — Phase 2 body calc (prototype) | `body.html` (new), `partials/navigation-bar.html`, `models/README.md` | **Claude** | Phase 2 of the looks tooling: a dedicated **Body Calc** nav page, built to the **same standards** as `looks.html` (on-device, two lenses, sex-aware bands, evidence-tiered honest measurement panel, trained-model headline + geometry breakdown/fallback, sharp voice). Jason's decisions (AskUserQuestion): **trained body model first** (geometry as the breakdown/fallback, same arc the face calc landed on) + **adiposity included as a hard-flagged visual proxy**. Engine: **MediaPipe Pose Landmarker** (33 BlazePose keypoints, on-device ESM `@0.10.14`) with `outputSegmentationMasks`. **Key insight:** BlazePose keypoints give joints but **no waist** (soft tissue, no landmark — the body's analog of "the hairline has no landmark"), so the page reads the **segmentation silhouette** — pose landmarks set the vertical levels (shoulder/hip lines), the silhouette gives the body's actual width at each → makes **waist-to-hip (Singh 1993)**, the **waist-to-height adiposity proxy (Tovée/Cornelissen, Tier 1)**, and the **shoulder-to-waist V-taper (Dixson)** measurable from one photo. Metrics (`poseSkeleton` + `bodyRatios`, pure/testable): WHtR, WHR, V-taper, shoulder:hip breadth, leg:torso (Sorokowski, Tier 3), symmetry; sex-conditional bands ({f,m}); `computeComposites` (proportional typicality + body dimorphism); `bodyMascIndex`→`guessSex` (body separates sex far better than a front face); `gradeBody` lens-weighted fallback composite; sex-aware Black Pill ladder (Dyel→Fit→aesthetic→Zyzz/IG-tier) + conventional tiers. ONNX inference layer architected (`MODEL_CONFIG`→`models/body-beauty.onnx`, full-res square person-crop ×1.15, ImageNet NCHW, gen-guard + NaN-guard from the looks lessons baked in from the start). Non-blocking reliability warnings (no-mask / arm-by-waist / tilted-stance). Honesty: loud caveat (clothing/pose/angle/depth confounds), "what a photo cannot measure" list (true BMI, muscle-vs-fat, absolute height, depth, under-clothing). **`models/README.md`** documents the body-model contract + the honest reality that — unlike SCUT for the face — **no turnkey body-beauty checkpoint exists** (train-a-regressor / repurpose / stay-on-geometry paths). Verified: node (27/28 pure-core assertions — skeleton math, silhouette ratios, sex guess both directions, composites, lens-divergent scoring, tiers, tensor norm, skeleton-only fallback; the 1 "fail" was a too-tight test bound, code correct) + live preview (pose model loads → Ready, UI renders, synthetic body → 7.9/10 "Chad-tier", sex toggle re-bases bands, 5 tiered sections, zero console errors). **Live pose+mask inference on a real photo untested** (needs a browser + a body image — Jason tests, same as the face handoff). face×body composite + a real `body-beauty.onnx` are the remaining phases. **WASM-abort crash safety (same day, two iterations):** Jason hit `Detection error: Aborted()` on a back-facing/arms-crossed shot — a MediaPipe native abort (likely the segmentation step) that *bricks the pose instance for the whole session* (every later photo then fails = the "crash"). **First attempt was wrong and made it worse:** a self-heal that recreated the landmarker and **retried the same image** — re-calling `detect()` on a poisoned WASM module doesn't throw, it **spins and freezes the page** ("Page Unresponsive"). **Lesson:** never re-run the crashing image, never do heavy work in the catch. **Final design:** a failed `detect()` (or mask read) just sets a `poseDead` flag + shows a friendly message + bails — no retry, no rebuild in the handler; the **next upload** (`analyzeFresh`) rebuilds the engine asynchronously during the natural file-pick pause, then scores the fresh image. The crashing image is never re-run; the crash handler is allocation-free so the main thread stays responsive. Mask read is try-guarded (degrade to skeleton-only + flag dead). Live-verified the refactor parses + pose model reaches Ready + no orphaned refs (the abort path itself needs a real triggering photo — Jason tests). **Caveat:** if a native first-`detect()` ever *hangs* (spins) instead of aborting, only a Web Worker + watchdog can keep the page alive — teed up as the next step if it recurs. **Not committed** (localhost only). |
| Body Calc — Web Worker isolation + the segmentation-abort root cause & fix | `body.html`, `js/body-pose-worker.js` (new) | **Codex** (worker + watchdog) · **Claude** (diagnosis + two-landmarker fix) | Jason was still hitting crashes/hangs after the `poseDead` design, so handed Codex a debug brief. **Codex's fix (worked, stopped the crash):** moved MediaPipe Pose into a **classic Web Worker** (`js/body-pose-worker.js`) with a main-thread **watchdog** (12s analysis / 45s init) that `terminate()`s a hung worker and respawns — the only architecture that survives a *native main-thread hang*, since JS can't interrupt a WASM spin in-thread. **But it introduced a new bug:** clean, front-facing full-body photos were **rejected** ("isolated pose engine stalled or aborted") — "most if not all" images. **Claude root-caused it in the Preview browser** (couldn't reproduce on the user's exact photos, so reproduced the *mechanism*): a no-pose image returns fast "No body found" (worker fine); a real pose (`mediapipe-assets/pose.jpg`) scores in ~55ms with a full silhouette (pipeline fine); but `mediapipe-assets/segmentation_input_rotation0.jpg` **rejects in 719ms with only a `detect-start` trace** — `detect()` threw before emitting landmarks. Captured error: **`Aborted(). Build with -sASSERTIONS for more info.`** at stage `detect`. **Proven with a two-worker A/B on the same image:** `outputSegmentationMasks:false` → `landmarks:33` ✅, `outputSegmentationMasks:true` → `Aborted()` ❌. **The mechanism:** MediaPipe computes the segmentation mask *inside* `detect()`, as part of the graph, **before the result callback fires** — so a segmentation hard-abort on certain bodies takes down the whole call before any landmarks exist → detect-stage reject, even though the pose is perfectly readable. Codex's mask-read `try/catch` guarded the wrong step (the cheap readback), not the fragile compute. **Claude's fix — two landmarkers on SEPARATE `FilesetResolver`s (verified to be isolated WASM instances: after `poseSeg` aborts, `posePlain` still returns `lm:33`):** `posePlain` (segmentation **off**) is the robust source of landmarks — never aborts, so a score is **always** produced; `poseSeg` (segmentation **on**) is the fragile silhouette enricher, tried first (one pass gets landmarks + mask when it works). On a seg `Aborted()`: catch it, fall back to `posePlain` for landmarks (→ **skeletal-only score**, honest "no silhouette mask" note — never a rejection), and flag `recycle` so the main thread **terminates + respawns** the worker (the only safe reset — re-creating a landmarker *in-process* on a just-aborted WASM **hangs the worker thread**, which an earlier in-worker-rebuild attempt proved by wedging the next image for 12s). The next image gets a fresh, healthy segmentation path. **Verified live across the realistic cycle:** seg-aborting image → 1.9/10 skeletal (not rejected); good image → full silhouette, fast (~750ms warm); abort → good again → **silhouette recovers** (fresh worker, ~1.4s); good images keep the worker warm (no needless respawn). Screenshot confirms the full silhouette breakdown (Adiposity/WHR/V-taper/shoulder-hip rows) + skeleton overlay render. **Not committed** (localhost only — Jason tests on his own photos). |
| Body Calc — scoring-quality triage: directional rubric, face sex-ID, bony-breadth units bug | `body.html` | **Claude** | The worker fix stopped the *rejections*, but Jason then saw the scores were wrong: a jacked top-10% man scored **2.1** while an average clothed man scored **4.0** (inversion), and a woman was **mis-sexed male** and underrated. Claude diagnosed **three independent defects** in the Preview browser, each reproduced with Jason's exact screenshot numbers. **(1) Directional-scoring bug → the inversion.** `scoreMeasure` was a *symmetric* gaussian on "closeness to the typical band", so an elite physique's broad shoulders (1.76) scored **6/100** — punished as a deviation exactly as hard as being narrow — and pose-noise symmetry (Tier 3) dominated. Most body cues are **monotonic toward one pole**, not optima. Fix: added a per-measure `dir` (`'low'` leaner-better / `'high'` bigger-better / `'band'` true-optimum, sex-conditional via a fn for whr+shoulderHip); `scoreMeasure` now uses a **saturating logistic** for directional cues (rewards the attractive direction, only penalizes the wrong one) and the gaussian only for true `'band'` optima. Symmetry made lenient (`tol:4`) + weight cut (0.6→0.3 BP / 0.9→0.5 conv). Result (hand-math matched exactly): jacked **2.1→8.2**, now above the average man. **(2) Silhouette is the real ceiling — confirmed, not fixed.** Live traces show MediaPipe pose segmentation returns **no usable mask on most real photos** (all three of Jason's, incl. the *shirtless* guy on a clean background): `detect-start → detect-complete`, no mask-read → skeletal-only, so the two Tier-1 drivers (leanness, WHR) and V-taper vanish and the score runs on the bony frame, which can rank but can't see muscle/fat. **(3) Face sex-ID (Jason's pick via AskUserQuestion: "face for sex-ID only").** Ported the Looks Calc's InsightFace **genderage** classifier (`models/face-sex.onnx`) into `body.html` — but cropping the face from the **pose's own face keypoints (BlazePose 0–10)**, so no second MediaPipe engine is needed. New `preprocessSex`/`sexFromLogits`/`FACE_PIDX` in the pure core; `SEX_MODEL_CONFIG`/`initSexModel`/`cropSexFaceTensor`/`inferSex` in the module; `bcSetSex` hook overrides the frame guess (manual flip still wins). Face-visibility gated. **Verified live:** on `segmentation_input_rotation0.jpg` (a woman the frame guesses **male**) the classifier reads **female, 99.7%** and the panel flips to Femininity — the exact mis-sex bug, fixed. Crop geometry verified to land on the head; small full-body faces are upscaled (less certain than the Looks Calc's large crops, manually flippable). **(4) Units bug the sex-fix unmasked.** Fixing sex revealed the woman then scored ~1.1 because the **bony** skeletal shoulder:hip (~1.6 for everyone) was being fed into **silhouette-calibrated** breadth bands — over-crediting every man, zeroing a correctly-identified woman. Removed the bony→silhouette fallback in `bodyRatios` (breadth is simply *unmeasured* with no silhouette — honest, and the panel row hides). Net after all four: jacked **7.0** > average **4.6** (cleaner gap; the average clothed man no longer inflated to 7.3), woman correctly female. The residual — that skeletal-only can rank but not assign trustworthy magnitude, esp. for women — is the **silhouette/CNN ceiling**, teed up as the next decision (recover the silhouette vs the face-beauty composite Jason deferred). Zero console errors. **Not committed** (localhost only). |
| Body Calc — silhouette FIXED via dedicated Selfie segmenter + strict input gating | `js/body-pose-worker.js` (rewritten), `body.html` | **Codex** (build) · **Claude** (verify) | Codex took the silhouette/generalization handoff brief and **solved it by sidestepping the abort entirely**: instead of fighting the pose landmarker's built-in segmentation (the `ChannelSize() (1 vs 4)` abort that no input form or version upgrade fixed — Codex confirmed it persists across `ImageBitmap`/`ImageData`, lite/full models, and versions `0.10.14`/`0.10.18`/`0.10.21`/`latest`), it runs the pose landmarker with `outputSegmentationMasks:false` (joints only, never aborts) and gets the silhouette from a **separate, dedicated MediaPipe task — the Selfie `ImageSegmenter`** (CPU, confidence mask). The Selfie mask returns **at input resolution**, so it's aligned to the landmarks out of the box — the square-256/257 misalignment that sank the GPU attempt is gone. Both tasks stay inside the disposable watchdog worker, so a native abort/spin still can't freeze the UI. **New `assessPose()` gate** (in the worker, in image pixels) refuses before scoring: malformed/incomplete landmarks, non-upright (torso-length + shoulder→hip vertical-drop ratio), cropped/uncertain feet (visibility + in-frame bounds), seated/crouched/bent legs (per-side thigh & shin verticality + drop), and side-on profiles (shoulder/hip breadth vs torso). `extractSilhouette()` seeds each width scan from the body center (`rowWidthNear`, robust to arms-out) and runs **plausibility bounds** on whr/taper/breadth/whtr; any failure → no silhouette. **`body.html` now refuses with a specific message instead of emitting a skeletal-only number** — a missing or implausible silhouette produces *no score*, killing the garbage 1.0s on partial/seated shots. **Claude independently verified live** (Codex couldn't access Jason's three private photos): both models init (`segmentation:true`); the **former CPU-abort image** (`segmentation_input_rotation0.jpg`) now completes silhouette extraction (~234ms) and scores **4.5 BP / 5.7 conv** with the **full Tier-1 panel populated** (WHtR 0.40, WHR 0.68, V-taper 1.32, shoulder:hip 0.90 — all silhouette-derived, blank before this fix) and the face classifier reading **female 100%**; the **bent-leg `pose.jpg`** is **cleanly refused** ("Seated, crouched, or strongly bent legs… use a neutral standing pose", **0 analyze calls, no score panel**); console clean of any *new* `Aborted()`/`ChannelSize` (the only such lines are stale, pre-reload). **Still Jason's to confirm on his own real photos** (the jacked-torso/fit-woman/seated-woman cases). Open: a real `models/body-beauty.onnx` (headline is still honestly badged "GEOMETRY SCORE — NO TRAINED MODEL LOADED") and the deferred face×body composite. **Not committed** (localhost only). |
| Body Calc — upper-body / torso read (grade cropped shots on frame cues) | `js/body-pose-worker.js`, `body.html` | **Claude** | Jason showed a 4-panel reddit example (ATTRACTIVE→UGLY men, all cropped at the hips) making the case that an upper body alone grades a male frame fairly — correct, because the discriminating signal there is shoulder breadth → V-taper, which a torso shot supports and which are **self-normalizing** (both widths in-frame, no height needed). Problem: those exact shots were **refused** by the full-body gate ("include the whole body from head to feet"). Added a **framing mode** so a clean, upright, frontal **upper-body shot scores instead of refusing**. Worker `assessPose` now returns `framing:'full'|'torso'` (and reordered: upright → hips-solid → frontal → feet-presence): cropped-but-upright (feet out of frame / low-confidence, hips still solidly in frame) → `framing:'torso'`; genuinely seated/bent-in-frame legs and side-profiles still **refuse**; chest-crops with the hips also gone refuse with an accurate "include down to the hips" message. `extractSilhouette` takes `framing` and **skips the head-to-ankle height** (and the WHtR plausibility bound) in torso mode — no scoring against an extrapolated, off-frame ankle. `body.html` drops `legTorso` + whole-body `symmetry` (both from off-frame joints) for torso framing; the composite (`gradeBody`/`computeComposites`) already renormalizes over present cues, so the torso read scores on exactly **WHR + V-taper + shoulder:hip**. Honesty: an amber **"Upper-body read"** caveat under the score (frame cues only; leanness needs full height; a draped top softens the waist; full body in fitted clothing reads more), the caption reads "Upper body measured", and the drop-zone/empty copy now says "full body, or upper body to the hips". **Design call (shared, badged scale):** the torso score uses the same 0–10 headline, clearly badged as a partial-basis read rather than a separate scale — flagged to Jason as tunable (a torso read is blind to height-normalized adiposity, one of the strongest drivers, so the magnitude can run optimistic; e.g. the same image full-body 4.5 vs torso-cropped 6.2). **Verified:** node (full→`full` all 6 cues; cropped→`torso`; chest-crop→`partial` refuse; `extractSilhouette` torso returns widths + `height:0`) **and** live in the Preview browser — full-body path unchanged (framing `full`, 6 keys, face female 100%); a synthesized torso crop (top 60% of `segmentation_input_rotation0.jpg`, legs removed, drag-dropped) scores **6.2 "Toned"** with framing `torso`, metrics exactly `[whr, vTaper, shoulderHip]`, the amber badge + "Upper body measured · female 99%" caption, adiposity/Tier-3 rows correctly absent, zero new console errors. **Not committed** (localhost only). |
| Body Calc — arm-corruption guard + CNN scoping (Connor OSF dataset) | `js/body-pose-worker.js`, `body.html`, `body-cnn-scoping-brief.md` (new) | **Claude** (build + diagnosis) · **subagent** (CNN research) | Jason tested real photos: a lean clothed **full-body woman scored 6.2** while a jacked **hand-on-hip man (torso) got 2.1** and an athletic woman 4.2 — the athletic bodies *under* the unbuilt one. Asked: bugs or algorithm limits? Diagnosed **both**, three mechanisms, from his own numbers: **(A) BUG — arms inflate the silhouette.** The width scan grows a contiguous body-pixel run from centre, so an arm touching/resting on the torso is counted as torso → waist & hip read fat → V-taper and shoulder:hip collapse. Tell: the man's WHR read *normal* (0.88) while V-taper (1.27) AND shoulder:hip (1.12 "narrow") both cratered — the exact signature of an arm alongside the torso inflating waist+hip together (his hand is literally on his hip). **(B) STRUCTURAL — torso reads lose the heaviest cue:** both athletic shots are upper-body crops → no waist-to-height/leanness score (the top-weighted driver), while the full-body woman banked a near-max leanness credit. So torso bodies systematically under-score full-body ones. **(C) CALIBRATION — leanness over-credits** (her whtr 0.29 pinned "very lean" drove the 6.2; provisional bands). **The limit underneath:** a 2D frontal silhouette of a posed, muscular body *cannot see muscle/depth* — geometry ranks frames, can't see an athletic build. AskUserQuestion → Jason chose **"both — fix geometry now + scope the CNN."** **Built (A):** `extractSilhouette` now flags an elbow/wrist sitting inside a measurement band AND tucked within `torsoHalf*1.1` horizontally → **drops that band's width** (waistArm→waistW=0, hipArm→hipW=0) so the dependent cue is dropped, not scored inflated; plausibility runs only on surviving ratios; returns `waistArm/hipArm`. `body.html`: the **waist read is required for a headline** (V-taper/WHR) — shoulder-breadth + Tier-3 alone is too thin (it produced a harsh, misleading 2.3 on the remnants) → if an arm hid the waist, **refuse with "arms a few inches out"** rather than score remnants; partial drops (hip only) score with a skip-warning. **Design call (flagged to Jason as tunable):** refuse-on-no-waist is the honest default but stricter — arms-down photos (incl. his 3 test images) now prompt arms-out instead of scoring a fake-wide waist; the threshold + refuse-vs-thin-read is a dial. The real fix for arms-down/athletic is the CNN. **Verified:** node (arms-out keeps all widths; arm-in-waist→waistW=0; hand-in-hip→hipW=0) + live (repro woman, arm by waist → clean refuse "arm across your waist… arms out", 0 score; `pose.jpg` still refuses for *bent legs* — gates stay distinct; zero new console errors). Live arms-out *positive* score best confirmed on Jason's own arms-out photo (no CORS-clean arms-out body image on hand; pipeline scoring proven in node + pre-fix runs). **CNN scoping (parallel subagent → `body-cnn-scoping-brief.md`):** no turnkey body-beauty checkpoint exists (confirmed; all physique-scoring products are closed SaaS). Recommended path = **fine-tune a torchvision ResNet18 on the Connor full-body stimuli set (OSF osf.io/egj7c — 726 clothed full-body images, attractiveness among 24 traits, ~490k ratings from 3,311 adults), export to ONNX with the exact SCUT recipe**, calibrate outMin/outMax from holdout quantiles → drops into the existing `models/body-beauty.onnx` slot, zero code change. Secondary: Celeb-FBI (height/weight→BMI proxy). Photofeeler-D3 = vaporware (proprietary). Caveats: N=726 (transfer-learn + augment + CV), 272 targets are head-swaps (decide to drop), dataset-bias/ethics flagged. **Not committed** (localhost only). |
| Body Calc — body-beauty CNN training recipe (Connor → ONNX) | `models/train_body_beauty.py` (new), `models/README.md` | **Claude** | Jason's call: **do the ONNX first, tune geometry after** (the CNN headline demotes geometry to breakdown, so tune against the model's verdict, not a system you're replacing — same arc as the face calc). Wrote a complete, Colab-runnable `train_body_beauty.py`: downloads the **Connor OSF full-body set** (`egj7c`), fine-tunes an ImageNet **ResNet18** (head+layer4 first, then full backbone) to regress mean attractiveness, **black-letterbox-square→224→ImageNet-RGB-NCHW** preprocessing **matched to body.html `cropBodyTensor`** (the page draws a square crop into a transparent→black canvas, so tall bodies get black side-bars — training mirrors that), reports held-out **Pearson/Spearman** (warns + says *don't ship* if Spearman < 0.30), exports ONNX with the **exact face-model contract** (`input`/`score`, opset 12, `dynamo=False`, `[1,3,224,224]`), runs an onnxruntime **parity check**, and **prints the calibrated `outMin`/`outMax`** (held-out 2nd/98th pct) to set in `MODEL_CONFIG`. **OSF schema then VERIFIED & baked in** (a subagent downloaded+extracted the 375MB `egj7c` zip and read the real headers): WIDE `aggregated_photo_ratings.csv` → `photo` + `attractiveness_mean`, **0–100 slider scale** (so MSE on raw targets; printed outMin/outMax come out ~0–100, not the face's 1–5), id↔file = filename stem with `-`↔`.` (`BM29-BM33.png`↔`BM29.BM33`), head-swaps = a `.` in the id (272/726). Script now direct-downloads the zip (`osf.io/download/khm9a/`) + extracts (no osfclient), with a defensive column-fallback + `--smoke` export check kept. `models/README.md` body section rewritten from "no turnkey, here are paths" → the concrete 3-step recipe + honesty caveats (N=726 small → transfer-learn; fixed US rater/target bias; 272 head-swaps droppable; clothed-silhouette not physique-under-clothing). Syntax-verified locally; torch not installed here so the live training/`--smoke` run is Jason's (Colab). Drops into the existing `models/body-beauty.onnx` slot with **zero `body.html` code change**. After this lands + calibrates, the deferred **geometry tuning** (arm-threshold, whtr band recalibration, cross-framing scale) resumes against the CNN headline. **Not committed** (localhost only). |
| Body Calc — CNN headline LIVE: trained, calibrated, gates restructured | `body.html` (+ `models/body-beauty.onnx`) | **Claude** (build) · **Jason** (trained the model in Colab) | Jason ran `train_body_beauty.py` in Colab on the Connor data → **val Spearman 0.606 / Pearson 0.605 / RMSE 10.5** (40 ep, ResNet18, 616/108 split, 724 pairs incl. 272 head-swaps kept; ONNX parity diff 3.8e-6). **0.606 is a strong result** for body-attractiveness-from-photo (faces get ~0.9; bodies are noisier) — well past the 0.30 ship floor. Dropped in `models/body-beauty.onnx` (44.7MB). **Claude verified in-browser** (onnxruntime-web): loads with input `input`/output `score` (no config tweak), discriminates (raw spread 11→35 across test imgs, no dead band). **Wired calibration:** `MODEL_CONFIG.outMin/outMax` 1.0/5.0 → **25.1/61.04** (the script's held-out 2nd/98th pct, 0–100 Connor scale). **Restructured the gates (the key change):** now that the CNN is the headline it scores from the whole-body crop and needs neither silhouette nor a readable waist — so the geometry "no outline" / "arm hid the waist" **refusals now fire ONLY when no model is loaded** (`!modelComing`, where `modelComing = session || modelState==='loading'`); with the model present those become breakdown caveats and an **arms-down / no-silhouette body still scores** (the whole point — geometry is demoted to the transparent breakdown, exactly Jason's CNN-first plan). Guarded the `px.waistArm` reads for the now-possible null-silhouette path; framing gate (bent/profile/seated) still refuses (model is OOD there). **Verified live:** the arm-by-waist woman that *refused* last turn now scores via the CNN (badge "Trained model score", geometry breakdown shows shoulderHip+Tier-3 with the arm caveat); her 1.0/10 is **correct** — the whole-image-crop diagnostic (raw 11.4) confirms she's a genuinely OOD two-person shot (woman holding a child), not a calibration bug. **Known minor nuance:** the page's pose-box crop reads ~6 pts above the whole-image letterbox the model trained on (modest train/infer framing shift — tunable). **Jason's turn:** test real single-person full-body photos (incl. his original 3: jacked man, athletic woman, lean woman) — does the spread read sensibly (lean/athletic high, soft low)? Then geometry tuning (whtr band ~2× high, arm threshold). **Not committed** (localhost only). |
| Body Calc — CNN real-photo calibration fix (clamping → sensible spread) | `body.html` | **Claude** | Jason's real-photo test: a flat **1.0** for average bodies, jacked still **4.0**, and the jacked guy mis-sexed female. Diagnosis from his 3 reported raws (avg man **18.5**, avg woman **20.4**, jacked **40.3**): the model **compresses real photos into ~18–40, well below its Connor-studio val range (25–61)** — so the val-percentile calibration (outMin 25.1) **clamped both averages to 1.0**. The model's *rank* is fine (jacked ≫ avg); only the anchor was wrong (distribution shift, real internet photos vs clean studio shots). **Fix:** re-anchored `MODEL_CONFIG.outMin/outMax` 25.1/61.04 → **−2/47** (negative floor needed because the model's real-photo band is narrow). Now: avg man ~3.9, avg woman ~4.4, **jacked ~8.7** (Black Pill; Conventional higher). **Verified live:** the OOD repro image (raw 17.48) went 1.0 → 3.6 (un-clamped, mapping correct). **Provisional** — fit from a few real raws; proper fix = a real-photo calibration batch (Jason runs ~10 varied bodies, reads the debug raw line, I set outMin/outMax from real percentiles). **Sex mis-ID is display-only** (headline is sex-agnostic) — the jacked guy's face is tiny in a full-body shot (genderage 72% = unreliable) AND his arm/silhouette frame guess also fails, so neither signal works; manual flip is the fallback. Deeper ceiling: 726 clean Connor shots → real-photo OOD compression; more/varied data is the long-term lever. **Not committed** (localhost only). |
| Body Calc — clothing-aware hybrid (CNN clothed / geometry shirtless) | `body.html` | **Claude** | Jason's 2nd real-photo batch exposed the model's hard limit: scores split by **clothing, not physique** — clothed bodies 18–23, shirtless/swimwear 36–40 *regardless of fitness* (overweight shirtless 38.9 ≈ jacked 38.9). The CNN is **clothed-trained (Connor)**, so a bare torso is OOD and it just outputs "skin ≈ high" → overrated an overweight man to 8.5, a skinny model to 8.1. The clothed case works (avg man 22.7 → 5.1 "felt accurate"). **Not a calibration bug — a data ceiling.** Key insight: geometry & CNN are **complementary** — CNN owns clothed (geometry confounded by clothing), geometry owns bare physiques (it measures the real V-taper/leanness/breadth the CNN can't). Jason (AskUserQuestion) chose the **auto-switch hybrid**. Built: `torsoSkinFraction(ctx,lm,W,H)` — **YCbCr** skin fraction of the central torso column (always body, no mask needed; computed *before* the skeleton overlay so red landmarks don't pollute it); `bareBody = skinFrac ≥ 0.45`; **`useModel = (session||loading) && !bareBody`** — CNN headline only for clothed, else the geometry silhouette is the headline (geometry gates re-apply). `inferAndSet` gated on `useModel`; `bcAnalyze` takes `bareGeometry`+`skinFrac`; new badge "Bare-physique read · the model is clothed-trained, so a shirtless/swimwear shot is scored from the silhouette geometry instead"; **debug readout** "torso skin = X% — clothed→model / bare→geometry (switch at 45%)" so Jason can tune the threshold like he did the raw output. **Verified live:** clothed woman → torso skin **0%** → CNN headline (3.6); YCbCr hand-checked (skin (200,150,120)→in-range ✓, blue shirt→out ✓). Shirtless→geometry branch not live-tested (no CORS-clean passing shirtless image) — symmetric logic + the geometry-headline path is the proven pre-CNN one; Jason validates on his shirtless photos. **Now geometry owns the shirtless headline → the deferred geometry tuning (whtr band ~2× high, arm threshold) matters again, esp. for bare physiques.** Shirtless *torso* still loses the adiposity cue (no full height) — full-body shirtless reads best. **Not committed** (localhost only). |
| Body Calc — Codex review applied, batch 2 (sex-range, grouped holdout, CNN uncertainty, router zone) | `body.html`, `models/train_body_beauty.py` | **Codex** (review) · **Claude** (build + verify) | The product-visible remainder of Codex's review. **(1) Sex-range headline (Finding 2 full fix).** A geometry headline is scored against sex-specific bands, so an undetermined sex can't honestly yield one number. `renderResult` now computes both bands when `usingRange = !usingModel && !sexKnown` and shows the **male–female range** (e.g. "4.2–8.1 / 10", tier "Depends on sex — ♂ 4.2 Dyel · unbuilt · ♀ 8.1 Stacy-tier") + a "Set sex to resolve the score" prompt; neither toggle is active; **collapses to a single score the moment sex is set** (manual/face). The model headline is sex-agnostic so it shows no range. **(2) Grouped-CV retrain (Finding 3).** `train_body_beauty.py` split was ungrouped random with 272 head-swaps kept → the same head/body could cross train/val, inflating the 0.606. Added `group_id_map` (union-find over the head-swap id tokens — any two images sharing a head OR body merge) and replaced the split with a **grouped split by body identity** (no component crosses the line); carries the id through `build_label_table`; warns if grouping forces an imbalanced fold. Python-compiled + unit-tested the grouping (shared tokens merge, originals isolated). **Jason re-runs in Colab — expect a lower but honest Spearman.** **(3) CNN uncertainty label (Finding 7 / R4).** Codex's call: **don't blend** two unvalidated signals — scope each instrument to its domain + show uncertainty. Added an "Experimental model — validated to *rank*, not pinpoint; read as a broad band (~±1.5)" note on every model headline. **(4) Router ambiguity zone (M4).** Kept the 0.45 default cutoff but added a 35–55% borderline band: a non-blocking "borderline clothed/bare — routing may be off" warning + a "borderline" flag in the skin debug readout. (Full manual clothed/bare override deferred — needs re-route plumbing; flagged to Jason.) Verified: node/python (grouping) + live (range renders + collapses on sex-set, experimental note on the real clothed-woman CNN run at 3.6/raw 17.476 unchanged, borderline flag toggles in/out of band, console clean). **Not committed** (localhost only). |
| Body Calc — Codex review applied, batch 1 (6 contained fixes) | `body.html`, `js/body-pose-worker.js` | **Codex** (review) · **Claude** (build + verify) | Codex returned a sharp 7-finding review of the scoring/rubric brief (validated M1–M5, R1–R5; confirmed the P0). Jason chose (AskUserQuestion): **demote WHtR** (keep, relabel, unscored) + **batch the contained code fixes with check-ins**. Landed 6: **(1) [P0] WHtR units bug** — [body.html:259](body.html:259) computes frontal waist *width*÷height but scored it against *circumference*÷height bands (don't convert; a normal body reads ~0.15–0.25 vs the 0.40–0.50 band → `dir:'low'` saturating logistic handed everyone ~99/100 leanness; the single largest driver of overrating BOTH extremes). Fix: `scored:false`+`uncalibrated:true` on the measure → `gradeBody` skips it, `computeComposites` keys drop it, and `measureRow` renders it as a raw proxy with an honesty note and **no band/marker** (relabeled "Frontal waist-width ÷ height (proxy) · Uncalibrated"). Node-verified the headline is now **fully invariant** to WHtR (0.15 vs 0.55 → identical 4.6110); live-verified the row renders value+note, no band/dot. **Consequence:** the geometry/shirtless headline now has no leanness term — the honest face of M1 (a front photo can't see depth fat). **(2) robust width extraction (Finding 5)** — worker `scanBand` took the absolute min(waist)/max(shoulder,hip) row, so one noisy row (fold, mask speck, arm tip) exaggerated taper/curviness; now a **trimmed quantile** (20th pct narrow / 80th pct broad). **(3) resolution/distance gate (M2)** — `drawAndScore` computes `personFrac` (body's share of frame) + native px; **refuses < 14%** ("too small/far"), **warns < 32%** or < 220 native px (directly addresses symptom 3, the far-shot athletic underrate). **(4) CNN-crop hardening (Finding 4)** — `cropBodyTensor` now clamps off-image joints to [0,1] and skips visibility < 0.3, so an extrapolated ankle / extended wrist can't blow up the square and shrink the torso. **(5) async routing race (Finding 6)** — `pendingTensor` was stashed unconditionally and run on model-load, overriding a bare body's geometry headline; now only stashed when `useModel`. **(6) sex honesty + unknown handling (Finding 2 / R3)** — face-crop now needs ≥ 40 native px (genderage is mush below that → prime mis-sex cause); `guessSex` returns **null in a deadzone** instead of silently defaulting female on a zero index; new "Sex undetermined" state (neither toggle active); and the copy that claimed sex "does not change the headline" now tells the truth — on the **geometry/bare-physique headline it does** (sex re-bases the bands `gradeBody` consumes). Verified: node (rubric core: WHtR skipped + score-invariant, composites finite, guessSex null/m/f) + live (clothed woman still CNN 3.6/raw 17.476 — routing intact; synthetic geometry-headline state renders the uncalibrated WHtR row correctly; console clean). **Still pending (next, product-visible):** the sex-range headline UI when sex is undetermined (Finding 2's full fix), grouped-holdout retrain of `train_body_beauty.py` (Finding 3, Jason re-runs), CNN-experimental uncertainty labeling (Finding 7/R4 — no blend, scope each instrument to its domain), and the router ambiguity zone (M4). **Not committed** (localhost only). |
| Body Calc — scoring/rubric second-opinion brief for Codex | `body-calc-scoring-review-brief.md` (new) | **Claude** (evaluation + brief) → **Codex** (review pending) | Jason reports the build is usable but still: (1) occasionally **mis-sexes**, (2) **overrates overweight AND underweight** bodies, (3) **underrates an athletic physique on a far shot**. Claude traced each to a specific mechanism and split them into **measuring-system vs rubric** causes: **(1 mis-sex)** the genderage classifier runs on a tiny BlazePose-keypoint face crop ([`cropSexFaceTensor` body.html:866](body.html:866)), unreliable on far/small/turned faces → falls back to `guessSex` from the bony frame which the code itself admits mis-reads sex; **(2a overweight)** a *frontal* silhouette is **blind to sagittal/depth fat** — a gut projects forward, a front-on waist-width scan sees it edge-on, so WHtR under-reads it; on clothed shots the weak CNN (Spearman 0.606 studio, lower OOD) carries the headline and doesn't push down enough; **(2b underweight)** the rubric's leanness cues are **monotonic** (`whtr dir:'low'`, male `whr dir:'low'` — [body.html:286](body.html:286)) with a saturating logistic that rewards leaner indefinitely (no inverted-U), so an emaciated frame banks near-max leanness credit, and a silhouette can't tell lean-athletic from skinny-no-muscle; **(3 far shot)** no resolution/scale gate — a distant subject → coarse mask + imprecise joints → width ratios compress toward 1.0 → V-taper flattens → athletic underrated, and `cropBodyTensor` upscales a tiny person box → blurry CNN. Also surfaced an **honesty bug**: "sex is display-only" is **false on the geometry-headline (shirtless) path** — sex re-bases the bands `gradeBody`/`computeComposites` consume, so on a bare body a wrong sex changes the headline ([body.html:530](body.html:530) overclaims). Wrote a detailed repo-root handoff `body-calc-scoring-review-brief.md` framing 5 measuring suspects (M1 depth-blind, M2 no res gate, M3 tiny face crop, M4 brittle 0.45 skin router, M5 muscle/fat conflation) + 5 rubric suspects (R1 monotonic leanness, R2 reasoned-not-fit bands/curves, R3 sex-affects-score honesty, R4 weak CNN owns headline, R5 hand-anchored calibration), naming R1/R3/R4 as highest-leverage, asking Codex to validate/refute each against the code and rank fixes by impact-per-effort (honest downgrade — warn/refuse/widen-uncertainty — counts). **No code change this turn** (evaluation + brief only). **Not committed** (localhost only). |
| Body Calc — silhouette root-cause dug out (CPU-abort), GPU rewrite tried & reverted, caption bug fixed | `body.html`, `body-calc-silhouette-review-brief.md` (new) | **Claude** (diagnosis) → **Codex** (handoff) | Jason pushed back that the eval is still off (jacked torso 1.0, fit woman 4.6, seated woman 1.0) and that I'd **band-aided** the standing case without generalizing — correct: on most real photos the calc runs skeletal-only, where the only cues are `legTorso` (breaks on cropped/non-standing framing — MediaPipe *extrapolates* missing ankles → garbage) and `symmetry` (≈always "asymmetric" noise). **Fixed the simple bug:** the status caption under the photo was set once with the frame guess and never updated; now rebuilt from state via `bcStatusNote()` (called from `bcAnalyze`/`bcSetSex`/manual-flip) so it tracks the face classifier (verified: shows "sex read from the face: female (100%)"). **Dug out the real root cause of the silhouette failure** (the actual ceiling) in the Preview browser: the worker's **`delegate:'CPU'` hard-aborts segmentation** — console shows `image_frame.cc:415 Check failed: 1 == ChannelSize() (1 vs 4)` then `Aborted()` (a channel-count mismatch, expects 1 gets 4=RGBA); A/B-proven (`outputSegmentationMasks:false`→33 lm ✅, `:true`→abort ❌). **GPU delegate doesn't abort** (same image → 33 lm + mask), **but** integrating main-thread GPU into body.html yielded **empty/misaligned masks** (extractSilhouette got 0 width at every landmark row; the mask is square 256/257 vs the image aspect; plus **WebGL context-exhaustion** degraded masks across repeated landmarkers), and **GPU-in-a-Worker returns an all-zero mask** (OffscreenCanvas GL readback is blank in worker scope — so you can't have both worker isolation AND the GPU silhouette). **Attempted the main-thread-GPU rewrite and REVERTED it** — it regressed (the worker silhouettes `pose.jpg`; the GPU path silhouetted nothing in-app), so I restored the known-good worker path rather than ship a non-delivering architecture change. Wrote a thorough **Codex handoff brief at repo root** (`body-calc-silhouette-review-brief.md`): the CPU `ChannelSize` abort lead, the GPU alignment/threshold/context issues, the worker-GL empty-mask constraint, and the strategic fork (make the silhouette reliable vs pivot the magnitude signal to the face-beauty CNN / a trained body CNN, geometry demoted to breakdown) + the input-gating that's needed regardless (reject/restrict partial-body & non-standing). **Net shipped this turn:** caption fix only; silhouette/generalization handed to Codex with a decisive diagnosis. **Not committed** (localhost only). |

| Body Calc — retrained CNN anchors + sex-classifier never-fired bug ROOT-CAUSED & fixed | `body.html` (+ retrained `models/body-beauty.onnx`) | **Claude** (diagnosis + fix) · **Jason** (Colab retrain) | Two things this turn. **(A) Wired the grouped-holdout retrain.** Jason's grouped-split run landed **honest Spearman 0.716 / Pearson 0.717 / RMSE 10.8** (ONNX parity exact, `|diff|=0`) — *higher* than the leaky 0.606, so head/body leakage was never inflating it; **settles Codex R4 → keep the CNN as the clothed-shot headline.** Dropped in the new `body-beauty.onnx` (44.7MB, same ResNet18 arch), set `MODEL_CONFIG.outMin/outMax` −2/47 → **26.234/69.261** (the trainer's held-out 2nd/98th-pct studio percentiles) as a START, with a comment warning these are *studio* anchors and the prev net compressed real photos ~10–20pt below its studio band — so real raws need a re-anchor pass. **CNN calibration still UNEXERCISED:** all 4 of Jason's test shots were shirtless/swimwear → routed to geometry (`torso skin` 75–100%), so the model never fired; needs a few **clothed** full-body shots to actually calibrate. **(B) Root-caused the mis-sexing (Jason's "ought to be fixed").** Symptom: 2 men read female, 1 athletic woman read male — every caption stuck on *"sex (provisional, from frame)"*, i.e. the face classifier **never overrode the unreliable frame guess** (the exact thing that mis-sexes broad-shouldered women / soft-waisted men). Proved the classifier **loads and runs fine** (instrumented `initSexModel`/`initModel` with load logs → both "loaded"; ran genderage in-page at 96² → clean 3-vec). So the failure was upstream: **`cropSexFaceTensor` returned null on every face.** Instrumented it (`window.__bcSex` recorder) + reproduced in the Preview browser by driving real public full-body photos through the actual file-input pipeline (Commons API, CORS `origin=*`): face read = **`face-too-small`**, `facePx 30, avgVis 0.998`. The gate `if (Math.max(bw,bh) < 40) return null` measured the **raw inner-face-keypoint bbox** (eyes/ears/nose/mouth — far smaller than the head), but the crop then pads it **1.9×** before sampling — so normal single-subject shots (bbox ~25–45px) were rejected and dumped on the frame guess. **Fix:** gate on the **sampled region** (`bbox × cropScale ≥ 40` native px), not the raw bbox. **Verified:** the exact 30px face that returned `face-too-small` now **fires** (`sex m, conf 0.869, sampledPx 57`), and the caption flips to *"Sex detected on-device from the face (87% confident)"*. Added a permanent `debug · face read = …` line (consistent with the existing skin / model-raw debug lines). **Jason re-tests his 3 misgendered images to confirm.** Honest remaining limits (NOT easy fixes, support the prototype-label + QOVES-link plan): overweight-overrated-on-shirtless (front silhouette is depth/adiposity-blind — the demoted-WHtR consequence) and far/tilted-pose underrating (ratios degrade off-axis, already warned). **Not committed** (localhost only). |

| Face × Body composite — "Overall looks/appearance rating" + Looks→Face rename + QOVES | `js/composite-score.js` (new), `partials/composite-section.html` (new), `css/styles.css`, `js/include.js`, `looks.html`, `body.html`, `partials/navigation-bar.html` | **Claude** (build + verify) · **4-agent map workflow** (integration recon) | Jason's long-deferred face×body composite, greenlit. Ran a **4-agent mapping workflow** (Explore agents over the face calc, body calc, the SMV↔Compatibility persistence pattern, and the nav/include/CSS scaffolding) → a precise integration map. Key correction to one agent's suggestion: a composite can't read live `window.LC`/`window.BC` because those globals live on **separate pages**; the right mechanism is **localStorage** (the SMV↔compat interop pattern). **Built:** (1) **rename Looks Calc → Face Calc** (label only — kept `looks.html` filename + `data-page="looks"` to avoid breaking links): nav-bar link, `<title>`, page-title, page-sub on both pages. (2) **`js/composite-score.js`** — shared module on both pages: each calc persists its latest headline (both lenses + each lens's `scaleMax`, source, sex) to `localStorage` (`loveEquations.faceScore.v1` / `bodyScore.v1`) via `window.leComposite.saveFace/saveBody`; the composite reads both, **normalises each calc to a 0–10 share of its own lens range** (face PSL maxes 8.6, body BP maxes 9 — normalising makes them comparable), blends **50/50 (provisional, tunable `FACE_WEIGHT`, labelled honestly)**, shows the Black-Pill Overall as hero + Conventional as the secondary line + a Face/Body breakdown + source attribution; prompts whichever calc is unscored; handles the body's **unresolved male–female range** as a `needsSex` "set a sex to resolve" prompt; **Reset** clears both keys. (3) **`partials/composite-section.html`** injected below the result on **both** pages (before the footer); (4) `include.js` now dispatches a **`partials:loaded`** event so the composite renders once its host lands; (5) composite CSS appended to `styles.css` (reuses the card/Playfair/scarlet tokens), CSS cache-version bumped `?v=1.6`→`1.7`; (6) **QOVES link** added to the Face Calc caveat as the "professionally-built" alternative (Jason: QOVES has a face rater; body uncertain). **Persist hook** = the tail of each page's `renderResult` (fires on every finalized score, incl. async model arrival + profile/sex changes — always current). **Verified live in the Preview browser, full cross-page flow with REAL photos:** scored a real face (John Kerry headshot → model 6.7/10) on the Face Calc → persisted + composite showed the face partial; **navigated to body.html → the composite already showed the carried face 6.7** (localStorage cross-page) + body prompt; scored a real body (geometry, male, 4.9/10) → composite computed the blended **5.8/10 "High-tier normie"** (Black Pill; Conventional 5.8 "Above average"), Face 6.7 & Body 4.9 weighted 50/50, source attribution correct; the blend math, `needsSex` path, partial states, and Reset (clears both keys) all checked exactly; CSS native (Playfair 73.6px slate hero, gradient panel); console clean (only benign MediaPipe XNNPACK info lines). **Not committed** (localhost only). **Open/tunable:** the 50/50 face/body weight (could go sex-conditional or a UI slider), and the composite currently follows no shared lens toggle (shows BP hero + Conventional line). |

| Body Calc — the REAL mis-sex cause: genderage confidently mislabels sunglasses-women → eye-occlusion guard | `body.html` | **Claude** (diagnosis + fix) | After the crop-gate fix, Jason re-tested his athletic-woman photo (white sportswear, **sunglasses**, hands on hips) → still "male". Reproduced the actual cause in the Preview browser by driving Commons "Women wearing sunglasses" photos through the live pipeline: the classifier now **fires** (crop gate fixed) but genderage **confidently mis-sexes women in sunglasses as male** — 2 independent photos read male at **97.5% / 99.4%** confidence, one on a huge clear 118px face. **Sunglasses remove the eyes, genderage's single strongest sex cue.** So it's neither cache nor the crop gate — it's a model limitation genderage can't overcome (it can't see through shades). **Fix = eye-occlusion guard** (`faceEyesOccluded`): samples luminance at the eye centres (BlazePose 2/5) vs the same face's cheeks (a **ratio**, so it's skin-tone-robust); a much-darker eye band ⇒ sunglasses ⇒ **decline to guess** — set sex `unknown` ("Sex undetermined — the eyes are hidden (sunglasses?)… set it below") with the male–female range + manual toggle, instead of asserting a confident-wrong male (`window.bcSetSexUnknown`, cleared on a fresh photo / manual pick). **Threshold reasoned from samples** (clear open eyes ~0.99 — sclera is bright; dark/reflective sunglasses 0.51–0.65) → **0.70**, biased to catch (a false "undetermined→set sex" is honest; a false "confident male" is the bug). **Verified live:** both sunglasses women (ratio 0.508 & 0.654) → undetermined + range + sunglasses copy; clear face (Obama, ratio 0.994) → still fires → male (no false-positive). **Then Jason hard-refreshed + re-dropped his exact photo → debug showed `face read = fired · face 25px, vis 1 → m 63%`:** the occlusion guard MISSED it because at a **25px face** the eye/cheek sample boxes are ~1px and can't resolve the shades — and genderage asserted male at only **63%** (near coin-flip). **Second guard added — a confidence gate** (`SEX_CONF_MIN = 0.75`): correct reads cluster ~0.87–1.0, so a sub-0.75 read is unreliable → `bcSetSexUnknown('the face is small or unclear … isn't confident (63%)')` instead of asserting. The two guards are complementary: occlusion catches **high-confidence** sunglasses misreads on big faces (genderage is confidently wrong there); the confidence gate catches **low-confidence** reads on small/ambiguous faces (his 25px case). Verified: Obama 0.869 → still fires male (no regression); the gate's `bcSetSexUnknown` end-state renders "Sex undetermined — … isn't confident (63%)" + range/toggle; console clean. The manual one-click "Read as female/male" remains the final backstop. **Not committed** (localhost only). |

| Face Calc — file rename `looks.html` → `face.html` + "Overall looks rating" title | `looks.html`→`face.html` (git mv), `partials/navigation-bar.html`, `body.html`, `js/composite-score.js`, `models/README.md`, `css/styles.css`, `partials/composite-section.html` | **Claude** | Follow-up to the composite row, which had kept the `looks.html` filename to avoid breaking links. Jason: "change looks.html to face.html and refactor, it's more logical." **`git mv looks.html face.html`** (history preserved), `data-page="looks"`→`"face"` on both the `<body>` and the nav-link (that pair drives the active-nav highlight in `include.js`), and every reference repointed: nav-bar href, `body.html` page-sub link, `composite-score.js` Face-Calc todo link + header comment, `models/README.md` contract path. Internal `window.LC` global left as-is (page-private, never user-facing). Also **simplified the composite title** "Overall looks & appearance rating" → **"Overall looks rating"** (`partials/composite-section.html`; matching comments in `composite-score.js` + `styles.css`). **Not committed** (localhost only). |

| Codex peer-review applied — composite floor/validation + honesty & robustness fixes | `js/composite-score.js`, `partials/composite-section.html`, `body.html`, `face.html`, `models/README.md`, `models/train_body_beauty.py`, `.gitignore` | **Codex** (review) · **Claude** (build + verify) | Codex peer-reviewed the uncommitted round (brief: `composite-gender-rename-review-brief.md`) and returned 7 findings; all sound, all applied. **[P1] Composite normalisation floor** — every scoring path returns `1 + (scaleMax-1)·x`, so scores live in `[1, scaleMax]`; the old `score/max·10` mapped the worst case to ~1.1, not 0. Fixed to `(score-floor)/(max-floor)·10`; payloads now carry `floor:1`, keys bumped `v1`→`v2` (retires mis-normalised v1). **[P1] Payload validation** — `validScore()` requires finite bp/cv/maxes/floor (a missing `cv` used to render a phantom 0.0); malformed → treated as unscored. **[P1] Sex-conflict** — face-female + body-male no longer silently blends under the face's sex; shows a scarlet warning + sex-neutral tier until they agree; added a "same person" caveat + relative save-age (`ago()`). **[P1] Retrain honesty** — it's a SINGLE grouped-HOLDOUT split (best epoch + calibration percentiles both off the same val set), not CV/independent-test → reworded `MODEL_CONFIG` comment + trainer docstring + this ledger ("grouped-CV"→"grouped-holdout"); dropped the implied "settles the headline-authority question". **[P1] WHtR overclaim** — the UI called it "a validated leanness index" while the config (correctly) says uncalibrated/unscored → relabelled "uncalibrated frontal waist-width-to-height proxy, displayed not scored". **[P2] Distance gate** — `personFrac` now skips `vis<0.3` joints (mirrors `cropBodyTensor`) so a stray extrapolated wrist/ankle can't inflate the box and admit a distant subject. **[P2] Stale README** — "Looks Calc"→"Face Calc" (x2), body anchors `1.0/5.0`→`26.234/69.261` (provisional). **Lower-pri also done:** composite a11y (`<h2>`+`aria-labelledby`+`aria-live`), red debug rows gated behind localhost-or-`?debug` (`BC_DEBUG`/`LE_DEBUG`), "% model confidence (not a calibrated probability)" wording, `__pycache__/` gitignored. **F1 accept-flaw:** Codex confirmed the muscle/fat/depth silhouette ceiling is the right accept-and-label call (global contrast is monotonic → can't reverse the athletic-5 vs overweight-5.4 ordering) AND added a second cause for the female miss — the sex bands encode one ideal apiece, so an athletic non-hourglass frame loses points to rubric bias, not just blindness; labelled that in the panel. **Deferred (Jason's call, NOT built):** a constrained weight-sensitivity pass + a labelled "Athletic frame" breakdown. **Verified live** (Preview): all 4 composite scenarios (floor-normalised blend 6.2, malformed→unscored, sex-conflict warning+neutral tier, min→0.0), a11y computed styles, both pages load console-clean. **Not committed** (localhost only) — awaiting Jason's sign-off.

| Body Calc — decisive sex + bold one-tap toggle (retire the "undetermined" states) + F1 note reword | `body.html` | **Jason** (QOL call) · **Claude** (build + verify) | After living with the calc, Jason preferred a **decisive** male/female call over "Sex undetermined" — much better QOL, with a prominent toggle to fix a wrong call. **Removed the two decline-to-guess paths:** (1) the confidence gate (`SEX_CONF_MIN 0.75` → `SEX_LOWCONF`, now only a debug tag, no longer gates) — a low-confidence read now commits and shows its % honestly; (2) the occlusion → unknown override — a sunglasses face now falls back to `bcAnalyze`'s decisive **body-frame guess** (which beats a confidently-wrong occluded-face read), not "undetermined". Every decisive read (model / frame-guess / manual) now appends a **bold scarlet one-tap** `Wrong? Switch to <other> →` link right after the confidence %, wired (`.bc-sexflip`) to re-base instantly. `sexSource='unknown'` now only in the genuine frame-deadzone (`guessSex` null). **Also reworded the committed F1 panel note** per the weight-sensitivity verdict: a fit body reading middling is an **input** problem (mis-read sex / degraded silhouette), NOT the rubric — the scorer ranks a clean, correctly-sexed read fine (dropped the overstated "rubric rewards the hourglass" framing). **Verified live** (Preview, driven via window hooks): the old 63%-undetermined case → decisive **female 7.5** + bold toggle; frame-guess path decisive + toggle; clicking the toggle re-bases **7.5 → 5.3** (exactly the sensitivity-pass mis-sex delta) and re-points to the other sex; console clean. **Not committed** (localhost only). |

| Composite — show RAW lens scores (match the calcs) + Black Pill/Conventional toggle + footer spacing | `js/composite-score.js`, `css/styles.css` | **Jason** (UX call) · **Claude** (build + verify) | Jason: the composite read **9.1** when the body calc showed Black Pill **8.3** / Conventional **9.3** — "from nowhere". Cause: the F2 floor-normalisation `(8.3-1)/(9-1)·10 = 9.1` (BP lens maxes 9, not 10) — mathematically principled for blending but it matches nothing on screen and even bumped the tier (Chad → Gigachad). **Fix: display + blend RAW lens scores** (the exact number each calc shows); **reverses the F2 *display* normalisation** — the fairness cost is negligible (face PSL ceiling 8.6 vs body BP 9, ~4%; Conventional both 10 so raw is exact) and the floor concern is moot when showing the native [1,scaleMax] score. Partial state now shows **8.3 / 9.3** matching the calc; full blend is the weighted avg of the displayed numbers (Face 7.0 & Body 8.3 → **7.7** "Chad", tier consistent again). **Added a Black Pill / Conventional lens toggle** (Jason's idea) styled like the calc toggles (slate/green active), lens persisted (`compositeLens.v1`); replaces the old "hero BP + Conventional secondary line". **`validScore` still requires the maxes/floor** (kept in the payload), so the v2 schema is unchanged. **Footer spacing:** `.composite-section` had `padding-bottom:0` → rested on the footer; set to 2.8rem (2.2rem mobile). **Verified live:** partial BP 8.3 / Conv 9.3 (match), toggle switches + persists, full blend 7.7 Chad, padding 44.8px, console clean. **Not committed** (localhost). |

| Calcs — persist the dropped photo + result across page switches (until replaced or reset) | `body.html`, `face.html`, `js/composite-score.js` | **Jason** (request) · **Claude** (build + verify) | Jason: the dropped image vanished on page switch; keep it until replaced by a new picture or a reset. **Each calc now persists its analysed photo + result to localStorage** (`bodyShot.v1` / `faceShot.v1` = `{img, state, ts}`) and **restores instantly on load** — no slow pipeline re-run. The snapshot is the overlaid canvas downscaled to a compact JPEG (max 768px, ~620px source → ~50-150KB); the state is the serialisable BC/LC analysis fields (metrics/geo+soft, model t, sex + source/conf/manual, framing, reliability, profile). **Two-script bridge:** the MediaPipe module owns the canvas (captures via `downscaleToDataURL` → `window.bcSetShot`/`lcSetShot`; repaints on restore); the UI-wiring script owns BC/LC + persistence (`bcSaveShot` at the `renderResult` tail, `window.bcRestoreState`/`lcRestoreState` set state + render). **Replace** = a new successful drop overwrites; a failed drop / `bcResetResult` clears (consistent with the empty panel). **Reset:** the composite button (relabelled **"Reset both (scores + photos)"**) now removes both shot keys + both score keys AND calls `window.bcResetAll`/`lcResetAll` to clear the on-page calc UI + canvas. `setItem` wrapped in try/catch (quota → skip, non-fatal). **Verified live both calcs:** drop→analyse→persist→reload→**restores the result + canvas image** automatically; composite Reset clears photos + scores + UI; console clean. **Privacy note (flagged to Jason):** the photo now lingers in on-device localStorage until reset — still "on your device", but persisted; a UI note may be worth adding. **Not committed** (localhost). |

*Going forward (straight-to-main workflow): name the builder in the work and keep
this table updated whenever a section is substantially built or reworked, since
the commit trailers won't disambiguate it.*
